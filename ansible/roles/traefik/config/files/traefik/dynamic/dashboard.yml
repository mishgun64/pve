---

http:
  routers:
    dashboard:
      rule: "Host(`traefik.mishgun.com`)"
      entryPoints:
        - websecure
      tls: {}
      service: api@internal
      middlewares:
        - auth
        - crowdsec

  middlewares:
    auth:
      basicAuth:
        users:
          - "admin:$apr1$GSYij9hk$qQHfl4XZxmZzTcp3FIBEp/"